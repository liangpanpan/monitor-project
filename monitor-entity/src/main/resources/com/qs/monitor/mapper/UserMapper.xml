<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qs.monitor.mapper.UserMapper">

    <select id="selectUser" resultType="com.qs.monitor.entity.User">
        select * from User where id = #{id}
    </select>

    <select id="selectUserByName" resultType="com.qs.monitor.entity.User">
        select * from User where username = #{username}
    </select>

    <insert id="insertUser">
        INSERT INTO
        `guxingyuan`.`User`(`username`)
        VALUES (#{userName});
    </insert>

    <update id="updateUser">
        update User
        set username = #{userName}
        where id = #{id}
    </update>

    <delete id="deleteUser">
        delete from User
        where id = #{id}
    </delete>
</mapper>
